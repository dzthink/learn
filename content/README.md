# content 目录说明

- `inbox/`：可选，存放从 Apple Notes / API 同步的原始文本。
- `drafts/`：由 ChatGPT 整理得到的 Draft 草稿。
- `ideas/`：文章大纲（Idea 层）。
- `articles/`：完整文章，带 frontmatter，可用于对外发布。
- `collections/`：文集/专题，按主题组织多篇文章。
- `derivatives/`：视频脚本、播客稿、各平台文案等。
- `assets/`：图片与封面等静态资源。
- `metadata/`：索引或状态记录（如 JSON/SQLite）。
